#create DB, establish connection
conn = sqlite3.connect('songs.db')
conn.close()

#cursor for statements
c = conn.cursor()


#create table
c.execute('''CREATE TABLE artists_X (id INTEGER PRIMARY KEY AUTOINCREMENT, author VARCHAR, song VARCHAR, lyrics TEXT);''')
conn.commit()

# insert rows
c.execute('''INSERT INTO  artists_X (author, song, lyrics) VALUES('X', 'X', 'XXXXXXXXXX');''')
conn.commit()

#
c.execute('''DELETE FROM artists_X
WHERE id>0;''')
conn.commit()

c.execute('''SELECT * FROM artists_X;''')
print(answer.fetchall())


c.execute('''SELECT name FROM sqlite_schema
WHERE type='table'
ORDER BY name;''')